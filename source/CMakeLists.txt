

AUX_SOURCE_DIRECTORY(./src/ LIBWORMNET_SRC)
ADD_LIBRARY (wormnet STATIC ${LIBWORMNET_SRC})
INCLUDE_DIRECTORIES(./)

INSTALL (TARGETS wormnet
	ARCHIVE DESTINATION lib
	)
INSTALL (DIRECTORY wormnet DESTINATION include/wormnet)
INCLUDE_DIRECTORIES(/usr/local/wormnet/include/)